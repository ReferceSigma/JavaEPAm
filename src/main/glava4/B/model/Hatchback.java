package main.glava4.B.model;

public class Hatchback extends Car {
    public Hatchback(String brand, String model, double price, double fuelConsumption, int maxSpeed) {
        super(brand, model, price, fuelConsumption, maxSpeed);
    }
}